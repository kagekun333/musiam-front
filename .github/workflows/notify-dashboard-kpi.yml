name: Notify dashboard to Slack (KPI) - CTA DEBUG ONLY

on:
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "step-1"
        shell: bash
